/* eslint-disable prettier/prettier */
/* eslint-disable @typescript-eslint/no-unused-vars */
import { Test, TestingModule } from '@nestjs/testing';
import { UpdatingController } from './updating.controller';
import { UpdatingService } from './updating.service';

describe('UpdatingController', () => {
 /*  let updatingController: UpdatingController;

  beforeEach(async () => {
    const app: TestingModule = await Test.createTestingModule({
      controllers: [UpdatingController],
      providers: [UpdatingService],
    }).compile();

    updatingController = app.get<UpdatingController>(updatingController);
  });

  describe('root', () => {
    it('should return "Hello World!"', () => {
      expect(updatingController.getHello()).toBe('Hello World!');
    });
  }); */
});
